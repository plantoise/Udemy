# Create a Question class with an __init__() methood with two attrributes: text and answer attributes.

class Question:
    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer